# Dataset

Download `spam.csv` from the SMS Spam Collection Dataset on Kaggle:
https://www.kaggle.com/datasets/uciml/sms-spam-collection-dataset

Place it in this folder as `dataset/spam.csv` before running `model.py`.

Format: CSV with columns `v1` (label: ham/spam) and `v2` (message text).
